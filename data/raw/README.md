# Raw Data

Place original source files here without editing them.

Expected default source file:

- `attacks.csv`

During setup, the raw files were moved into:

`F:\shark-attack-api\data\raw`

Copy those files into this directory before running:

```powershell
python scripts/inspect_source.py
python scripts/export_public.py
```
