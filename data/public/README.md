# Public Data

Generated sanitized outputs are written here:

- `incidents_public.csv`
- `shark_attacks.sqlite`
- `schema.json`, if requested from inspection tooling

These files are ignored by Git because they can be rebuilt from `data/raw`.

